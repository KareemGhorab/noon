import { FC } from 'react'

type TProps = {
	welcomeText: string,
	mainText: string
}

const NavbarAccountClient: FC<TProps> = ({mainText, welcomeText}) => {
	return <div>
		
	</div>
}

export default NavbarAccountClient
